# #Connectwise Manage Public Endpoints
#
# No description provided (generated by Openapi Generator https://github.com/openapitools/openapi-generator)
#
# The version of the OpenAPI document: 2025.1
#
# Generated by: https://openapi-generator.tech
# Generator version: 7.12.0
#

require 'date'
require 'time'

module ConnectWise
  class ProjectTicketNote
    attr_accessor :id, :note_type, :ticket, :text, :detail_description_flag, :internal_analysis_flag, :resolution_flag,
                  :time_start, :time_end, :bundled_flag, :merged_flag, :issue_flag, :original_author, :member, :contact, :_info

    class EnumAttributeValidator
      attr_reader :datatype, :allowable_values

      def initialize(datatype, allowable_values)
        @allowable_values = allowable_values.map do |value|
          case datatype.to_s
          when /Integer/i
            value.to_i
          when /Float/i
            value.to_f
          else
            value
          end
        end
      end

      def valid?(value)
        !value || allowable_values.include?(value)
      end
    end

    # Attribute mapping from ruby-style variable name to JSON key.
    def self.attribute_map
      {
        id: :id,
        note_type: :noteType,
        ticket: :ticket,
        text: :text,
        detail_description_flag: :detailDescriptionFlag,
        internal_analysis_flag: :internalAnalysisFlag,
        resolution_flag: :resolutionFlag,
        time_start: :timeStart,
        time_end: :timeEnd,
        bundled_flag: :bundledFlag,
        merged_flag: :mergedFlag,
        issue_flag: :issueFlag,
        original_author: :originalAuthor,
        member: :member,
        contact: :contact,
        _info: :_info
      }
    end

    # Returns attribute mapping this model knows about
    def self.acceptable_attribute_map
      attribute_map
    end

    # Returns all the JSON keys this model knows about
    def self.acceptable_attributes
      acceptable_attribute_map.values
    end

    # Attribute type mapping.
    def self.openapi_types
      {
        id: :Integer,
        note_type: :String,
        ticket: :TicketReference,
        text: :String,
        detail_description_flag: :Boolean,
        internal_analysis_flag: :Boolean,
        resolution_flag: :Boolean,
        time_start: :Time,
        time_end: :Time,
        bundled_flag: :Boolean,
        merged_flag: :Boolean,
        issue_flag: :Boolean,
        original_author: :String,
        member: :MemberReference,
        contact: :ContactReference,
        _info: :'Hash<String, String>'
      }
    end

    # List of attributes with nullable: true
    def self.openapi_nullable
      Set.new(%i[
                note_type
                detail_description_flag
                internal_analysis_flag
                resolution_flag
                bundled_flag
                merged_flag
                issue_flag
              ])
    end

    # Initializes the object
    # @param [Hash] attributes Model attributes in the form of hash
    def initialize(attributes = {})
      unless attributes.is_a?(Hash)
        raise ArgumentError,
              'The input argument (attributes) must be a hash in `ConnectWise::ProjectTicketNote` initialize method'
      end

      # check to see if the attribute exists and convert string to symbol for hash key
      acceptable_attribute_map = self.class.acceptable_attribute_map
      attributes = attributes.each_with_object({}) do |(k, v), h|
        unless acceptable_attribute_map.key?(k.to_sym)
          raise ArgumentError,
                "`#{k}` is not a valid attribute in `ConnectWise::ProjectTicketNote`. Please check the name to make sure it's valid. List of attributes: " + acceptable_attribute_map.keys.inspect
        end

        h[k.to_sym] = v
      end

      self.id = attributes[:id] if attributes.key?(:id)

      self.note_type = attributes[:note_type] if attributes.key?(:note_type)

      self.ticket = attributes[:ticket] if attributes.key?(:ticket)

      self.text = attributes[:text] if attributes.key?(:text)

      self.detail_description_flag = attributes[:detail_description_flag] if attributes.key?(:detail_description_flag)

      self.internal_analysis_flag = attributes[:internal_analysis_flag] if attributes.key?(:internal_analysis_flag)

      self.resolution_flag = attributes[:resolution_flag] if attributes.key?(:resolution_flag)

      self.time_start = attributes[:time_start] if attributes.key?(:time_start)

      self.time_end = attributes[:time_end] if attributes.key?(:time_end)

      self.bundled_flag = attributes[:bundled_flag] if attributes.key?(:bundled_flag)

      self.merged_flag = attributes[:merged_flag] if attributes.key?(:merged_flag)

      self.issue_flag = attributes[:issue_flag] if attributes.key?(:issue_flag)

      self.original_author = attributes[:original_author] if attributes.key?(:original_author)

      self.member = attributes[:member] if attributes.key?(:member)

      self.contact = attributes[:contact] if attributes.key?(:contact)

      return unless attributes.key?(:_info)

      if (value = attributes[:_info]).is_a?(Hash)
        self._info = value
      end
    end

    # Show invalid properties with the reasons. Usually used together with valid?
    # @return Array for valid properties with the reasons
    def list_invalid_properties
      warn '[DEPRECATED] the `list_invalid_properties` method is obsolete'
      []
    end

    # Check to see if the all the properties in the model are valid
    # @return true if the model is valid
    def valid?
      warn '[DEPRECATED] the `valid?` method is obsolete'
      note_type_validator = EnumAttributeValidator.new('String', %w[TicketNote TimeEntryNote MeetingNote])
      return false unless note_type_validator.valid?(@note_type)

      true
    end

    # Custom attribute writer method checking allowed values (enum).
    # @param [Object] note_type Object to be assigned
    def note_type=(note_type)
      validator = EnumAttributeValidator.new('String', %w[TicketNote TimeEntryNote MeetingNote])
      unless validator.valid?(note_type)
        raise ArgumentError, "invalid value for \"note_type\", must be one of #{validator.allowable_values}."
      end

      @note_type = note_type
    end

    # Checks equality by comparing each attribute.
    # @param [Object] Object to be compared
    def ==(other)
      return true if equal?(other)

      self.class == other.class &&
        id == other.id &&
        note_type == other.note_type &&
        ticket == other.ticket &&
        text == other.text &&
        detail_description_flag == other.detail_description_flag &&
        internal_analysis_flag == other.internal_analysis_flag &&
        resolution_flag == other.resolution_flag &&
        time_start == other.time_start &&
        time_end == other.time_end &&
        bundled_flag == other.bundled_flag &&
        merged_flag == other.merged_flag &&
        issue_flag == other.issue_flag &&
        original_author == other.original_author &&
        member == other.member &&
        contact == other.contact &&
        _info == other._info
    end

    # @see the `==` method
    # @param [Object] Object to be compared
    def eql?(other)
      self == other
    end

    # Calculates hash code according to all attributes.
    # @return [Integer] Hash code
    def hash
      [id, note_type, ticket, text, detail_description_flag, internal_analysis_flag, resolution_flag, time_start,
       time_end, bundled_flag, merged_flag, issue_flag, original_author, member, contact, _info].hash
    end

    # Builds the object from hash
    # @param [Hash] attributes Model attributes in the form of hash
    # @return [Object] Returns the model itself
    def self.build_from_hash(attributes)
      return nil unless attributes.is_a?(Hash)

      attributes = attributes.transform_keys(&:to_sym)
      transformed_hash = {}
      openapi_types.each_pair do |key, type|
        if attributes.key?(attribute_map[key]) && attributes[attribute_map[key]].nil?
          transformed_hash["#{key}"] = nil
        elsif type =~ /\AArray<(.*)>/i
          # check to ensure the input is an array given that the attribute
          # is documented as an array but the input is not
          if attributes[attribute_map[key]].is_a?(Array)
            transformed_hash["#{key}"] = attributes[attribute_map[key]].map do |v|
              _deserialize(::Regexp.last_match(1), v)
            end
          end
        elsif !attributes[attribute_map[key]].nil?
          transformed_hash["#{key}"] = _deserialize(type, attributes[attribute_map[key]])
        end
      end
      new(transformed_hash)
    end

    # Deserializes the data based on type
    # @param string type Data type
    # @param string value Value to be deserialized
    # @return [Object] Deserialized data
    def self._deserialize(type, value)
      case type.to_sym
      when :Time
        Time.parse(value)
      when :Date
        Date.parse(value)
      when :String
        value.to_s
      when :Integer
        value.to_i
      when :Float
        value.to_f
      when :Boolean
        if value.to_s =~ /\A(true|t|yes|y|1)\z/i
          true
        else
          false
        end
      when :Object
        # generic object (usually a Hash), return directly
        value
      when /\AArray<(?<inner_type>.+)>\z/
        inner_type = Regexp.last_match[:inner_type]
        value.map { |v| _deserialize(inner_type, v) }
      when /\AHash<(?<k_type>.+?), (?<v_type>.+)>\z/
        k_type = Regexp.last_match[:k_type]
        v_type = Regexp.last_match[:v_type]
        {}.tap do |hash|
          value.each do |k, v|
            hash[_deserialize(k_type, k)] = _deserialize(v_type, v)
          end
        end
      else # model
        # models (e.g. Pet) or oneOf
        klass = ConnectWise.const_get(type)
        klass.respond_to?(:openapi_any_of) || klass.respond_to?(:openapi_one_of) ? klass.build(value) : klass.build_from_hash(value)
      end
    end

    # Returns the string representation of the object
    # @return [String] String presentation of the object
    def to_s
      to_hash.to_s
    end

    # to_body is an alias to to_hash (backward compatibility)
    # @return [Hash] Returns the object in the form of hash
    def to_body
      to_hash
    end

    # Returns the object in the form of hash
    # @return [Hash] Returns the object in the form of hash
    def to_hash
      hash = {}
      self.class.attribute_map.each_pair do |attr, param|
        value = send(attr)
        if value.nil?
          is_nullable = self.class.openapi_nullable.include?(attr)
          next if !is_nullable || (is_nullable && !instance_variable_defined?(:"@#{attr}"))
        end

        hash[param] = _to_hash(value)
      end
      hash
    end

    # Outputs non-array value in the form of hash
    # For object, use to_hash. Otherwise, just return the value
    # @param [Object] value Any valid value
    # @return [Hash] Returns the value in the form of hash
    def _to_hash(value)
      if value.is_a?(Array)
        value.compact.map { |v| _to_hash(v) }
      elsif value.is_a?(Hash)
        {}.tap do |hash|
          value.each { |k, v| hash[k] = _to_hash(v) }
        end
      elsif value.respond_to? :to_hash
        value.to_hash
      else
        value
      end
    end
  end
end
